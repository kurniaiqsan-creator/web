RewriteEngine On

# Jangan rewrite file/directory yang beneran ada
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route ke index.php di root (bukan public/)
RewriteRule ^(.*)$ index.php [QSA,L]
